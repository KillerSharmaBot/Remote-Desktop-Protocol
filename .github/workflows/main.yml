name: SAY GEX

on:
  workflow_dispatch:
    inputs:
      code:
        description: PAY GORN
        required: true

jobs:
  build:
    name: SAYGEXPAYGORN
    runs-on: windows-latest

    steps:
    - name: Disable Firewall Profiles
      run: |
        Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False

    - name: Install Necessary Packages
      run: |
        choco install chrome-remote-desktop-host -y --ignore-checksums
        choco install googlechrome -y --ignore-checksums

    - name: Download MEmu
      run: |
        $memu_url = "https://www.memuplay.com/download/MEmu-Setup.exe"
        $output = "$env:TEMP\MEmu-Setup.exe"
        Invoke-WebRequest -Uri $memu_url -OutFile $output
        Write-Host "MEmu downloaded to: $output"

    - name: Install MEmu (Silent)
      run: |
        $installer = "$env:TEMP\MEmu-Setup.exe"
        if (Test-Path $installer) {
          Start-Process -FilePath $installer -ArgumentList "/S" -Wait
          Write-Host "MEmu installation completed"
        } else {
          Write-Host "MEmu installer not found"
        }

    - name: Start Chrome Remote Desktop
      continue-on-error: true
      run: |
        $paths = @(
          "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe",
          "${Env:PROGRAMFILES}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
        )
        foreach ($path in $paths) {
          if (Test-Path $path) {
            & $path
            break
          }
        }

    - name: Start Remote Desktop Session
      continue-on-error: true
      run: |
        ${{ inputs.code }} --pin=123456

    - name: Keep Connection Active
      run: |
        $i = 8080
        while ($i -gt 0) {
          Write-Host $i
          Start-Sleep 60
          $i--
        }
